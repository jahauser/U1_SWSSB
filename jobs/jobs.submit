universe              = vanilla
executable            = jobs/run.sh
arguments             = $(params)

# Send only what the job needs
transfer_input_files  = jobs, src, Project.toml, Manifest.toml, QCSB

should_transfer_files   = YES
when_to_transfer_output = ON_EXIT
transfer_output_files   = output

# Logs (on submit node)
output                = logs/$(Cluster).$(Process).out
error                 = logs/$(Cluster).$(Process).err
log                   = logs/$(Cluster).log

+JobDurationCategory = "Medium"

# Resources (tune)
request_cpus          = 4
request_memory        = 8 GB
request_disk          = 5GB

# Container (pick ONE)
container_image       = osdf:///ospool/ap40/data/jacob.hauser/julia-U1_SWSSB-1.12-20251230.sif

# One job per line in params.txt
queue params from jobs/params.txt